export enum ApiErrorKey {
  UNAVAILABLE_ORIGIN_BALANCE = 'UNAVAILABLE_ORIGIN_BALANCE',
  UNKNOWN = 'UNKNOWN',
}

export enum ApiErrorTranslations {
  UNAVAILABLE_ORIGIN_BALANCE = 'Não há animais dessa categoria nesse lote.',
  UNKNOWN = 'Ocorreu um erro ao processar sua solicitação.',
}
